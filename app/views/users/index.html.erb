<% provide(:title, "All Users") %>
<h1>Search for Users</h1>

<%= will_paginate %>
  <ul class="users">
    <% @users.each do |user| %>
      <li>
        <%= gravatar_for(user, size: 50) %>
        <%= link_to full_name(user), user %>
      </li>
    <% end %>
  </ul>
<%= will_paginate %>